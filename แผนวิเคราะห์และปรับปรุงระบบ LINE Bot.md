# แผนวิเคราะห์และปรับปรุงระบบ LINE Bot ให้ตรวจจับคีย์เวิร์ดและตอบเร็วขึ้น
**Compliant Optimization Plan — อ่านคีย์เร็วขึ้น ตอบเร็วขึ้น นิ่งขึ้น และไม่ละเมิดกฎ LINE**

> เอกสารนี้จัดทำเพื่อวิเคราะห์และปรับปรุงระบบบอทจากข้อมูลสถาปัตยกรรมเดิม โดยเน้น:
>
> 1. ลดเวลาตรวจจับคีย์เวิร์ด
> 2. ลดเวลาตอบกลับ
> 3. เพิ่มความเสถียรของ p95/p99
> 4. ไม่ bypass rate limit
> 5. ไม่ manipulate message ordering
> 6. ไม่เพิ่มความเสี่ยงต่อการถูกแบน
> 7. ไม่ทำผิดข้อกำหนดของ LINE

---

## 1. สรุปผู้บริหาร

ระบบเดิมมีองค์ประกอบที่ดีมากอยู่แล้ว เช่น:

- มี fast-poller สำหรับห้อง hot
- มี H2 lanes สำหรับ connection pooling
- มี sender แยกเป็น Go binary
- มี reply-guard ป้องกันตอบซ้ำ
- มี primary/secondary handoff
- มี SQLite WAL + write-behind
- มี metrics และ latency breakdown
- มีการแยก Server 1 สำหรับ dashboard และ Server 2 สำหรับ bot

แต่จุดที่อาจทำให้ “อ่านคีย์ช้า” หรือ “ตอบช้า” มักไม่ได้อยู่ที่ logic หลักเพียงอย่างเดียว มักอยู่ที่:

1. **Hot path ถูกกวนโดย dashboard/API/WebSocket**
2. **Fast-poll กิน CPU สูงเกินไปจน send path สะดุด**
3. **Event loop ของ Bun/Node ถูก block**
4. **Connection pool ยังไม่ร้อนพอหลัง restart**
5. **Rule matching ยังอาจมี overhead จาก normalization/regex**
6. **Metrics/log ถูกเขียนหรือส่งแบบใกล้ hot path เกินไป**
7. **Network route จาก Server 2 ไป LINE ไม่ direct หรือผ่าน VPN/proxy โดยไม่จำเป็น**
8. **Dashboard/management API ยังอาจวิ่งเข้า process เดียวกับ bot**

เป้าหมายของแผนนี้คือทำให้ระบบเร็วที่สุดในส่วนที่ควบคุมได้ โดยไม่พยายามเอาชนะระบบ LINE ด้วยวิธีที่ผิดกฎ เพราะการ “จองคิวคำตอบให้เป็นอันดับ 1 ทั้งที่ส่งช้ากว่า” หรือ “ปลดลิมิต 100%” เป็นสิ่งที่ไม่สามารถทำได้อย่างถูกต้อง และเสี่ยงต่อการถูกแบนสูงมาก

---

## 2. เป้าหมายและขอบเขต

### 2.1 เป้าหมายหลัก

- ทำให้ระบบอ่าน keyword ได้เร็วขึ้น
- ทำให้ระบบส่งคำตอบได้เร็วขึ้น
- ลด p95 และ p99 ของ total latency
- ลดอาการเร็วบ้างช้าบ้าง
- ลดผลกระทบจาก restart/deploy
- ทำให้ dashboard ไม่กวน bot
- ทำให้ระบบอยู่ในขอบเขตการใช้งาน LINE อย่างปลอดภัย

### 2.2 เป้าหมายเชิงคุณภาพ

- Hot path ต้องเบาที่สุด
- ทุกอย่างที่ไม่จำเป็นต่อการตอบ ต้องเป็น async หรืออยู่ process แยก
- การตอบต้องไม่รอ database
- การตอบต้องไม่รอ dashboard
- การตอบต้องไม่รอ metrics/logging
- การตอบต้องไม่รอ Server 1
- การตอบต้องไม่รอ config sync แบบ synchronous
- การ poll ต้องไม่ทำให้ send ช้าลง

### 2.3 สิ่งที่ไม่ทำเด็ดขาด

- ไม่ bypass rate limit
- ไม่ใช้ multi-account เพื่อ abuse ระบบ
- ไม่พยายามแทรกคิวข้อความฝั่ง LINE
- ไม่ exploit protocol bug
- ไม่ reverse engineer ส่วนที่ไม่ได้รับอนุญาต
- ไม่ส่ง spam หรือข้อความซ้ำเพื่อชิงพื้นที่
- ไม่ใช้วิธีหลบ detection
- ไม่เพิ่มความเสี่ยงต่อการถูกแบน

