# เทคนิคทำให้บอทตอบเร็วและนิ่ง — สรุปจากโปรเจกต์นี้

เอกสารนี้สรุปทุกเทคนิคที่ใช้จริงในโปรเจกต์ LINE OpenChat auto-reply bot ตัวนี้
เพื่อให้ตอบข้อความเร็วที่สุดเท่าที่ทำได้ และนิ่งที่สุดเท่าที่ทำได้ ใช้เป็นแม่แบบ
ไปสร้างระบบอื่นที่ต้อง "แข่งตอบสนองให้ทันคู่แข่ง" แบบเดียวกันได้

หลักการรวมข้อเดียวที่คุมทุกอย่างข้างล่างนี้: **แยกให้ชัดว่าเวลาไหนคุมได้ เวลาไหนคุมไม่ได้
แล้วทุ่มความพยายามเฉพาะส่วนที่คุมได้** — เวลาที่คุมไม่ได้ (ฝั่งเซิร์ฟเวอร์ปลายทาง/เครือข่าย)
ต่อให้เขียนโค้ดดีแค่ไหนก็แก้ไม่ได้ อย่าไปเสียเวลาตรงนั้น

---

## สถานะระบบที่ใช้งานจริง — อัปเดต 1 กันยายน 2026

หัวข้อนี้คือ **แหล่งอ้างอิงหลัก** สำหรับระบบที่ deploy อยู่ในตอนนี้ ส่วนหัวข้อเชิงแนวคิด
ด้านล่างใช้ประกอบการตัดสินใจเท่านั้น หากขัดกันให้ยึดหัวข้อนี้และโค้ดเป็นหลัก

### เป้าหมายที่วัดถูกต้อง

คำว่า “ตอบก่อน” ไม่ได้แปลว่า request ส่งออกเร็วอย่างเดียว แต่คือ:

`เวลา LINE สร้างข้อความ → เราเห็น event → claim ได้ → LINE รับคำตอบ`

เราไม่มีสิทธิ์ควบคุมจังหวะที่ LINE ส่ง event ให้แต่ละบัญชี จึงรับประกันว่าจะชนะทุกข้อความไม่ได้
สิ่งที่ระบบทำคือ ลดเวลาที่คุมได้, มีเส้นทางรับมากกว่าหนึ่งเส้น, และเก็บหลักฐานว่าเส้นไหนเห็นก่อนจริง

### เทคนิคที่เปิดใช้อยู่

1. **Dedicated Room Poller สำหรับห้อง hot**
   - 1 async chain ต่อ `bot + Square room` พร้อม sync token ของตัวเอง
   - ใช้ `fetchSquareChatEvents` ตรงถึงห้องบน connection อุ่น และเว้นอย่างน้อย 100ms ระหว่างรอบ
     เพื่อไม่ให้ poll ต่อเนื่องแย่งเลนส่ง
   - จำกัดด้วย `SQUARE_FAST_POLL_MAX_ROOMS` ที่ runtime cap เป็น 1 ห้องต่อ LINE session

2. **Push + normal poll + dedicated poll แข่งกันรับ แต่ตอบได้ครั้งเดียว**
   - ห้อง hot ได้ทั้ง `dedicated-poll` และเส้นทางปกติ; ห้องทั่วไปยังได้ `push`/`normal-poll`
   - event ทุกทางเข้า handler เดิม ไม่มีกฎหรือ sender ชุดพิเศษ
   - `message-id` dedupe ตั้งแต่ประตูเข้า และ room/reply claim กันตอบซ้ำ
   - ถ้ามีหลายบอทของเจ้าของเดียวกันในห้องเดียวกัน ตัวที่เห็น event และ claim ได้ก่อนเป็นผู้ตอบ;
     ตัวอื่นหยุดที่ claim จึงไม่เกิดข้อความรัว

3. **ห้ามมี poll ซ้อนในห้องเดียวกัน**
   - `SQUARE_FAST_POLL_WORKERS` ถูก cap เป็น **1** ในโค้ด แม้ .env เดิมตั้งมากกว่านั้น
   - การเปิด 8 workers ในห้องเดียวสร้าง 8 cursor ที่แย่ง H2 lanes และขยาย error เดียวเป็น 8 retry loops;
     ไม่ได้ทำให้รับ event ได้เร็วขึ้นอย่างมีประโยชน์

4. **HTTP/2 lanes ที่เราเลือกเอง**
   - `LINE_TRANSPORT=hybrid` ใช้ Bun/Go ตามงานที่เหมาะสม และมี H2 pool ไป LINE ที่รักษา connection
     ให้ warm
   - เลือก lane จาก RTT/สถานะ/in-flight แทนปล่อยให้ fetch เลือกแบบสุ่ม
   - `LINE_H2_SEND_RESERVED_LANES` กันเลนส่งจาก traffic poll ได้ แต่เป็นค่าที่ต้อง A/B test;
     ห้ามเพิ่มเพราะเดา
   - การเลือก lane ตอนส่งจริงใช้ "พยากรณ์เวลาเสร็จ" ไม่ใช่แค่ RTT ล่าสุด + มี relative cooldown
     และ per-bot lane pin — ดู 3.6

5. **Hot path ใน memory**
   - กฎ compile/cache ล่วงหน้า, การตั้งค่าห้องและสิทธิ์ lookup จาก memory, claim/dedupe เป็น Map แบบ TTL
   - งานเหล่านี้ไม่ต้องรอ database ก่อนยิงคำตอบ

6. **ป้องกันการค้างและ auth error**
   - dedicated poller มี timeout, initial drain เพื่อไม่ตอบข้อความเก่า, backoff และ AbortController
   - start/stop/relogin เริ่ม–หยุด–reset poller โดยอัตโนมัติ
   - `AUTHENTICATION_FAILURE` หรือ permission denial หยุด chain ของห้องนั้นทันทีและแจ้งให้ login ใหม่;
     ไม่ยิง retry รัวจนทำให้ระบบอื่นช้า

7. **วัด source ที่ชนะจริง**
   - ทุก event ระบุ `dedicated-poll`, `normal-poll` หรือ `push` ใน fast-path metrics/dashboard
   - เก็บเวลาขาเข้า, เวลาทำ rule/claim และเวลาที่ LINE ตอบกลับแยกกัน
   - ใช้ timestamp ที่ LINE สร้างข้อความเข้าและข้อความตอบเพื่อเทียบผลกับบอทอื่น; ไม่เทียบกับ
     log “poll interval” ของคู่แข่ง เพราะนั่นอาจเป็นแค่ค่าที่เลื่อน slider

### ค่าที่ต้องยึด (ชื่อจริงในโค้ด, default ในโค้ด ณ 1 ก.ย. 2026)

```ini
# ห้อง hot: หนึ่ง cursor ต่อ bot + ห้องเสมอ (MAX_ROOMS hard-cap = 1 ในโค้ด)
SQUARE_FAST_POLL=1
SQUARE_FAST_POLL_INTERVAL_MS=100          # floor 100; 50/0 ต้องเปิด ALLOW_* ควบคู่
SQUARE_FAST_POLL_ALLOW_50MS=0
SQUARE_FAST_POLL_MAX_ROOMS=1
SQUARE_FAST_POLL_WORKERS=1

# transport/lanes: เริ่มจากค่าที่วัดแล้ว; เปลี่ยนทีละตัวและ A/B test
LINE_TRANSPORT=hybrid
LINE_H2_LANES=6
LINE_H2_SEND_RESERVED_LANES=0            # 0 = ปิด; ต้องวัดผลก่อนเปิด
LINE_H2_LANE_MAX_AGE_MS=900000
LINE_H2_LANE_RECYCLE_GAP_MS=60000

# SEND lane policy (dispatch/lane-speed-policy.ts, send-prediction.ts — ดู 3.6)
LINE_H2_SEND_SLOW_FLOOR_MS=23            # ประวัติ 28→23→20→23; ห้ามลดจาก sample ช่วงสั้น
LINE_H2_SEND_SLOW_RATIO=1.5
LINE_H2_SEND_SLOW_COOLDOWN_MS=15000
LINE_H2_SEND_PIN_ENTER_MS=21
LINE_H2_SEND_PIN_EXIT_MS=23
LINE_H2_SEND_SAMPLE_WINDOW=7
LINE_H2_SEND_JITTER_WEIGHT=0.35

# แบ่งบอทหลาย process (ถ้าใช้): static owner list หรือ balanced-sticky
WORKER_ASSIGNMENT_MODE=                  # "balanced-sticky" เพื่อเปิดโหมดแบ่งอัตโนมัติ
WORKER_TOPOLOGY_FILE=                    # หรือคุมทั้ง topology จากไฟล์ JSON เดียว
```

`SQUARE_FAST_POLL_MAX_ROOMS` เพิ่มจำนวน **ห้อง** ที่ได้ dedicated poll ไม่ใช่เพิ่ม cursor ในห้องเดิม
(ตอนนี้ hard-cap ไว้ที่ 1 ในโค้ด และ "จำนวนห้องที่เปิดได้ต่อบอท" ผูกจากค่านี้โดยตรง — เพิ่มเมื่อ
CPU, account quota และ latency p95 ยังดีอยู่เท่านั้น และต้องแก้ที่โค้ด ไม่ใช่แค่ env)

### สิ่งที่ไม่ทำ เพราะช้าหรือเสี่ยงกว่า

- ไม่เพิ่ม workers/cursors ในห้องเดียวกัน
- ไม่เพิ่ม poller ชุดสองซ้อนกับ Dedicated Room Poller
- ไม่ใช้ raw request ที่ผูก ID ห้อง/บัญชีตายตัว หรือ fallback ที่ข้าม sender ปกติ
- ไม่ถือว่า log `✓` ของโปรแกรมอื่นเป็นหลักฐานส่งสำเร็จ; ต้องยืนยันจาก LINE/metric ของเรา
- ไม่ไล่ลด delay อย่างเดียวโดยไม่ดู inbound source, p50/p95 และ error/rate-limit

### วิธีพิสูจน์ว่าเร็วขึ้น

1. ใช้ห้องทดสอบเดียวกัน, กฎและข้อความทดสอบเดียวกัน, และบัญชีที่ online จริง
2. เก็บอย่างน้อย 50 ข้อความต่อการตั้งค่า และสลับ A/B หลายช่วงเวลา
3. ดู `receiveSource`, inbound delay, code p95, LINE total p95, จำนวน duplicate และ error auth
4. เก็บเฉพาะการเปลี่ยนที่ p95 ไม่แย่ลงและมีหลักฐานว่า dedicated-poll เห็น event ก่อนบ่อยขึ้น

---

## 1. การตรวจจับข้อความให้เร็ว (Inbound / Detection)

### 1.1 แข่งสองทางพร้อมกัน แล้วตัดข้อความซ้ำทีหลัง
อย่าพึ่งกลไก "แจ้งเตือน" อย่างเดียวถ้ามันช้า/batch เอง (หลายแพลตฟอร์ม batch การแจ้งเตือนไว้
~100ms โดยไม่บอกใคร) ให้เปิด **สองเส้นทางพร้อมกัน**:
- เส้นทางแจ้งเตือนปกติของแพลตฟอร์ม (push/webhook) — เป็น fallback ที่วิ่งตลอดเวลา
- เส้นทาง polling เฉพาะจุดที่ยิงรัวๆ เฉพาะห้อง/ช่องทางที่สำคัญ

ใครเจอข้อความก่อนก็ทำงานก่อน ตัดข้อความซ้ำด้วย message-id (ดู 1.4)

### 1.2 Polling ต้อง "เฉพาะจุด" ไม่ใช่ "ครอบคลุมหมด"
ห้อง/ช่องทางที่ยิง poll ถี่ๆ ทุกช่องพร้อมกันจะแย่งทรัพยากรกันเอง (ดู 3.3) — จำกัดจำนวน
ห้องที่ poll ถี่ต่อบัญชี/ต่อ process (budget) แล้ว**เลือกห้องที่สำคัญที่สุดเข้าโควตานั้น**

**กับดักที่เจอจริง**: ถ้าเลือกห้องด้วยการ "จัดอันดับความคึกคักที่ผ่านมา" ห้องที่ปกติเงียบ
(แล้วอยู่ๆ มีข้อความสำคัญโผล่มา) จะไม่มีวันติดอันดับ ก่อนข้อความนั้นจะมาถึง — ไก่กับไข่
**ทางแก้ที่ใช้จริง**: ทำให้ "จำนวนห้องที่อนุญาต" เท่ากับ "โควตา poll" พอดี ผลคือทุกห้องที่
เปิดไว้ได้ poll ครบเสมอ ไม่ต้องแข่งอันดับกันเลย (เพราะไม่มีใครถูกตัดออก)

### 1.3 บอทหลายตัว (ถ้าระบบรองรับหลายบัญชี) แข่งกันตรวจจับ แต่ตอบทีเดียว
ถ้าใช้ได้หลายบัญชี/หลาย process ตรวจจับพร้อมกันในช่องทางเดียวกัน (จังหวะ poll ต่างกันคนละ
milliseconds) จะมีโอกาสเจอข้อความเร็วสุดในฝูงสูงขึ้น — **แต่ต้องมีระบบเลือกว่าใครตอบจริง
ตัวเดียว** (ดู 3.4) ไม่งั้นข้อความเดียวกันจะถูกตอบซ้ำหลายครั้ง

**อย่าให้บอทที่ตรวจจับ "หลบ" ห้องที่บอทอื่นคุมอยู่แล้ว** ถ้าระบบเลือกผู้ตอบทีเดียวอยู่แล้ว
(3.4) การกองบอทหลายตัวไว้ห้องเดียวกันคือกำไรล้วนๆ (รอบตรวจจับอิสระเพิ่มขึ้น) ไม่ใช่ความเสี่ยง

### 1.4 กันตรวจจับซ้ำจากสองเส้นทาง
เก็บ message-id ที่เพิ่งเห็นไว้ใน memory (Map + TTL) เส้นทางไหนเจอก่อนได้ไปต่อ อีกเส้นทาง
ที่เจอทีหลังถูกข้ามไปเงียบๆ — ทำเป็น cache ในหน่วยความจำ ไม่ใช่ query ฐานข้อมูลทุกครั้ง

### 1.5 ทุก fetch ต้องมี timeout
ถ้า connection ค้าง (เซิร์ฟเวอร์รับ request แล้วไม่ตอบกลับ ไม่ error ไม่ timeout เอง) loop
ตรวจจับจะค้างตลอดกาลแบบเงียบๆ ไม่มี error ให้จับ ไม่มี log — **นี่คือสาเหตุพบจริงของอาการ
"ห้องเงียบไปดื้อๆ 20+ นาที ทั้งที่ระบบดูออนไลน์ปกติ"** ห่อทุก fetch ด้วย timeout (`Promise.race`
กับตัวจับเวลา) แล้วปล่อยให้ loop เดิม retry ตาม backoff ที่มีอยู่แล้ว

---

## 2. การประมวลผลให้เร็ว (Processing)

### 2.1 Compile กติกาการจับคู่ล่วงหน้า ไม่ใช่ทุกข้อความ
ถ้าใช้ regex/keyword matching ต้อง **compile ครั้งเดียวตอน config เปลี่ยน** เก็บไว้ใน memory
cache ห้ามคอมไพล์ใหม่ทุกข้อความที่เข้ามา (ช้ากว่ากัน 10-20 เท่าในทางปฏิบัติ)

### 2.2 เช็คสิทธิ์/การตั้งค่าจาก memory ไม่ใช่ฐานข้อมูล
คำถามที่ถามทุกข้อความ ("ห้องนี้เปิดใช้งานไหม" "ต้องเป็น admin ไหม") ต้องตอบได้จาก
`Map`/`Set` lookup (O(1)) ไม่ใช่ query ฐานข้อมูลทุกครั้ง — sync กับฐานข้อมูลตอนมีการแก้ config
เท่านั้น

### 2.3 วัดจริงว่าอะไรกินเวลา แยกเป็นส่วนๆ
เก็บ breakdown ของเวลาแต่ละ phase แยกกัน (ตรวจจับ/ถอดรหัส/จับคู่/เช็คสิทธิ์/เตรียมส่ง/ส่งจริง)
ไม่ใช่แค่เวลารวม — เวลาส่วนใหญ่มักไปกองอยู่ที่ "ส่งจริง" (เครือข่าย) ไม่ใช่โค้ดเราเอง การรู้ว่า
ส่วนไหนกินเวลาจริงกันไม่ให้เสียเวลาไปจูนส่วนที่ไม่ใช่ปัญหา

---

## 3. การส่งให้เร็ว (Outbound / Send)

### 3.1 คุม connection pool เอง ไม่ใช้ default ของ library
ถ้าปล่อยให้ HTTP client เลือก connection ให้เองอัตโนมัติ จะไม่มีทางบอกได้ว่า "อย่าใช้เส้นนี้
มันเพิ่งมีปัญหา" ทำ pool ของตัวเอง (จำนวนจำกัด เช่น 6-10 connection ต่อบัญชี) คุม lifecycle
เองว่าเส้นไหนอุ่น (เพิ่งใช้สำเร็จ) เส้นไหนควรเลี่ยง

### 3.2 แยกเลนสำหรับ "ส่ง" ออกจากเลนสำหรับ "ตรวจจับ" (ถ้าจำเป็น)
ถ้า poll กับ send ใช้ connection pool เดียวกัน การ poll รัวๆ จะแย่งเลนกับตอนต้องส่งจริง —
พิสูจน์แล้วว่าทำให้ส่งช้าลงกว่าเท่าตัวได้ (วัดจริง: 16.6ms → 35.5ms) ทางแก้คือกันเลนบางส่วนไว้
สำหรับส่งอย่างเดียว **แต่ต้องวัดผลจริงก่อนคงค่าไว้ถาวร** (ลองมาแล้วสองรอบ ไม่ช่วยเสมอไป)

### 3.3 จำกัดจำนวน stream พร้อมกันต่อบัญชี
ยิ่ง concurrent request เยอะบน connection pool เดียวกัน ยิ่งแย่งกันเอง — ทดสอบจริงแล้วว่า
เพิ่มจาก 1 เป็น 2 concurrent ทำให้ช้าลงเท่าตัว ไม่ใช่เร็วขึ้น อย่าเข้าใจว่า "ยิ่งขนานเยอะยิ่งเร็ว"
เสมอไปเวลาทรัพยากรที่ใช้ร่วมกันมีจำกัด

### 3.4 ระบบ "ตัวจริงตอบ ตัวอื่นแค่สอดแนม"
ถ้ามีหลายบัญชี/หลาย process ที่อาจตรวจจับข้อความเดียวกันได้ (1.3) ให้**ทุกตัวตรวจจับเต็มสปีด
ตามปกติ แต่พอเจอแล้วให้ส่งไม้ต่อให้ "ตัวหลัก" ที่กำหนดไว้เป็นคนยิงจริง** (เรียกฟังก์ชันภายใน
process เดียวกัน ไม่ใช่ยิง network ใหม่ — ต้นทุนแทบเป็นศูนย์) ได้ประโยชน์สองต่อ:
- ภาพที่คนอื่นเห็นคือมีผู้ตอบคนเดียวเสมอ (ไม่มีใครสังเกตได้ว่าเป็นเจ้าของเดียวกันหลายบัญชี)
- ความเร็วเท่าเดิม (ไม่มี network hop เพิ่ม)

**สำคัญ**: ระบบ rate-limit/quota การส่งต้องผูกกับ "บัญชีที่ส่งจริง" ไม่ใช่ "บัญชีที่ตรวจจับ"
ไม่งั้นบัญชีตัวจริงจะโดนบัญชีสอดแนมหลายตัวเร่งจนเกินโควตาของตัวเองโดยไม่มีอะไรเช็คทัน

### 3.5 ใช้ protocol แบบกระชับที่สุดที่ปลายทางรองรับ
ถ้าปลายทางมี "แบบเต็ม" กับ "แบบย่อ" ของ request เดียวกัน (เช่น field น้อยกว่า, ไบต์น้อยกว่า)
ใช้แบบย่อเสมอเมื่อทำได้ ลดเวลา serialize/transfer ต่อ request

### 3.6 เลือก connection ด้วย "พยากรณ์เวลาเสร็จ" ไม่ใช่ RTT ล่าสุด + relative cooldown + pin

เมื่อมี connection pool หลายเส้น การเลือกเส้นที่ "RTT ล่าสุดต่ำสุด" มีปัญหา: RTT เส้นเดียวแกว่งเอง,
และเส้นที่ in-flight เต็มอยู่จะเสร็จช้ากว่าที่ตัวเลข RTT บอก ระบบนี้เลยเลือกด้วย **เวลาที่คาดว่า
จะเสร็จ** ต่อ (บัญชี + เส้นทาง): `predicted = p50 + (p95−p50)×0.35 + queueMs` จาก sample ล่าสุด
~7 ครั้ง โดย `queueMs` โผล่เฉพาะเมื่อ in-flight เกิน concurrent-stream capacity ที่ปลายทางโฆษณา
(ไม่มี "ค่าปรับต่อ request" ที่เดาเอา) ถ้าไม่มี sample สดของเส้นนั้นก็ถอยไปใช้ transport PING

**Cooldown ต้องเป็น relative ไม่ใช่ค่าตายตัว**: พักเส้นที่ช้ากว่า *ตัวใดตัวหนึ่ง* ระหว่าง floor
สัมบูรณ์ กับ 1.5× เส้นที่เร็วสุดในรอบนั้น (อันไหนแคบกว่าใช้อันนั้น) — pool ที่เร็วอยู่แล้วจะกด
มาตรฐานตัวเองให้สูง ส่วนตอน upstream ช้าทั้งแผง (ทุกเส้นช้าหมด) ต้อง **fail-open** กลับไปใช้เส้นที่
เร็วสุดที่มี ไม่ใช่พักทุกเส้นจนส่งไม่ได้ **กับดักจริง**: floor สัมบูรณ์ที่ตั้งชิด median เกินไป
(ห่างแค่ ~0.1ms จาก p50 ที่วัดได้ 7 วัน) พังภายใน 1 ชม.บน production — พอ RTT จริงขยับนิดเดียว
floor เริ่ม cool ตัว median เอง แล้วพอหลายเส้น cool พร้อมกัน pool จัดอันดับไม่ได้เลย ต้องเผื่อ
margin เหนือ median จริงๆ และเปลี่ยน floor นี้ทีละตัว (ไม่ bundle กับฟีเจอร์อื่น) เพื่อให้ regression
โยงกลับมาที่ตัวเลขนี้ได้

**per-bot lane pin (hysteresis)**: จำเส้นที่บอทหนึ่งเคยวัดได้เร็วมาก (< enter threshold) ไว้ให้
มัน **ชนะเฉพาะกรณีเสมอจริง** (คู่แข่งอยู่ในระยะ ~0.1ms) แทนที่ round-robin จะหมุนออก จนกว่า score
ของมันเองจะแตะ exit threshold (เช่น 21/23ms) — เส้นที่เร็วกว่า *จริง* ชนะ pin เสมอ ถ้าถ่างช่องนี้
กว้างกว่าแค่ tie-break เส้นเดียวจะกินทุก send ของบอทนั้น แล้ว reserved lane อื่นจะเย็นจนวัดไม่ได้
(regression ที่ round-robin มีไว้กันตั้งแต่แรก)

---

## 4. ความนิ่ง/ทนทาน (Reliability)

### 4.1 Watchdog เช็คเป็นระยะ ไม่ใช่รอ error
ทำ timer แยกที่เช็คว่า "เงียบมานานผิดปกติหรือยัง" (เทียบกับเวลาล่าสุดที่มีกิจกรรม) แทนที่จะ
รอให้ error โผล่มาเอง เพราะ connection ที่ค้างแบบไม่ error คือของจริงที่เจอบ่อยที่สุด

### 4.2 กู้คืนแบบขั้นบันได ไม่กระโดดไปขั้นแรงสุดทันที
1. ลองซ่อมแบบเบาที่สุดก่อน (เช่น ยิง request ใหม่บน connection เดิม)
2. ถ้าไม่หาย ปิด connection ให้ตัวเองต่อใหม่
3. ถ้ายังไม่หายหลายรอบ ถึงจะรื้อ session ทั้งหมด (แพงที่สุด ใช้เป็นทางเลือกสุดท้าย)

แต่ละขั้นมี cooldown กันยิงซ้ำรัวๆ และต้อง**เช็คให้แน่ใจว่าการซ่อมรอบก่อนเกิดขึ้นจริง**ก่อนนับ
cooldown (กับดักที่เจอจริง: นับว่า "ซ่อมแล้ว" ทั้งที่ไม่มีอะไรให้ซ่อมเลย เพราะ connection หาย
ไปแล้วตั้งแต่ต้น ทำให้เกิดช่วงตาบอดยาวโดยไม่มีใครรู้)

### 4.3 ผูกค่าคงที่ที่ต้องสอดคล้องกันไว้ด้วยกันจริงๆ
ถ้ามีค่าคงที่สองตัวคนละที่ในโค้ดที่ "บังเอิญ" ต้องเท่ากันเพื่อให้ระบบทำงานถูก (เช่น เพดาน
ทรัพยากร ต้องไม่เกินโควตาที่ใช้ทรัพยากรนั้น) **อย่าปล่อยให้บังเอิญตรงกัน** — ให้ตัวหนึ่งอ้างอิง
มาจากอีกตัวโดยตรงในโค้ด (ไม่ใช่แค่คอมเมนต์บอกว่า "อย่าลืมให้เท่ากันนะ") แล้วเขียน test ล็อกไว้
ว่าต้องเท่ากันเสมอ กันบั๊กที่กลับมาแบบเงียบๆ ตอนมีคนแก้ค่าใดค่าหนึ่งทีหลังโดยไม่รู้ว่ามันผูกกัน

### 4.4 Log เหตุการณ์ผิดปกติแยกจาก log กิจกรรมปกติ
เหตุการณ์ที่ควรมีคนมาดู (ส่งไม่สำเร็จ, ช้าผิดปกติ, connection ค้าง) ต้องแยกตาราง/ช่องทาง
จาก log กิจกรรมทั่วไป (เริ่ม/หยุด/ออนไลน์) ไม่งั้นจะจมหายไปในปริมาณ log ปกติ

---

## 5. วินัยการวัดผล (Measurement Discipline)

### 5.1 วัด "เวลาที่แข่งกับคู่แข่งจริง" ไม่ใช่แค่เวลาของตัวเอง
เวลาที่คู่แข่งแข่งด้วยจริงๆ = (เวลาที่ต้นทางส่งข้อความมา จนกว่าระบบเราจะรู้ตัว) + (เวลาที่เรา
ใช้ตอบ) — วัดแค่ครึ่งหลังแล้วบอกว่า "เราเร็ว" นั้นหลอกตัวเองได้ง่ายมาก บอทที่ตอบใน 20ms
แต่รู้ตัวช้าไป 80ms แพ้บอทที่ตอบ 50ms แต่รู้ตัวตรงเวลา โดยที่ dashboard ของเราเองมองไม่เห็น
ความแพ้นั้นเลยถ้าวัดแค่ครึ่งหลัง

### 5.2 ห้ามสรุปจากช่วงเวลาเดียว
Latency แกว่งได้เองตามธรรมชาติ 20-60ms+ ในวันเดียว ไม่เกี่ยวกับการเปลี่ยนแปลงอะไรเลย —
ต้องเทียบแบบสลับช่วงเวลา (A ช่วงหนึ่ง, B ช่วงหนึ่ง, กลับมา A อีกครั้ง, ...) ไม่ใช่ "ลองค่าเก่า
ตอนเช้า ลองค่าใหม่ตอนบ่าย" เพราะเวลาของวันเองก็เป็นตัวแปรที่ปนเข้ามาได้ ต้องการตัวอย่างขั้นต่ำ
(เช่น 50+ ตัวอย่าง จาก 3+ ช่วงเวลาที่ไม่ติดกัน) ก่อนเชื่อผลได้

### 5.3 เก็บ timestamp จากนาฬิกาต้นทางไว้เทียบกับคู่แข่ง
ถ้าเก็บเวลาที่แพลตฟอร์มประทับให้ทั้งข้อความที่เข้ามา**และ**ข้อความที่เราตอบ (ไม่ใช่นาฬิกา
ของเราเอง) จะเทียบความเร็วกับคู่แข่งได้ตรงๆ (คำตอบของคู่แข่งก็เป็นแค่ "ข้อความเข้า" ธรรมดา
ที่มี timestamp เดียวกันชนิดกับของเรา) โดยไม่ต้องรู้อะไรเกี่ยวกับระบบของคู่แข่งเลย

---

## 6. โครงสร้างพื้นฐาน / ระดับ OS (นอกเหนือจากโค้ดแอป)

- **ที่ตั้งเซิร์ฟเวอร์**: เช็คเส้นทางเครือข่ายจริง (BGP looking glass) ไปยัง ASN ของปลายทาง
  ก่อนเลือก region — ระยะทาง/จำนวน hop คือเพดานที่โค้ดแก้ไม่ได้เลย
- **เครื่องเฉพาะ (dedicated/bare-metal)** แทน cloud VM ที่แชร์ทรัพยากร ลด jitter จาก
  "เพื่อนบ้าน" บนเครื่องเดียวกัน
- **จูน kernel**:
  - `net.ipv4.tcp_slow_start_after_idle = 0` — สำคัญมากถ้าระบบพยายามคง connection ให้
    "อุ่น" ไว้ (3.1) เพราะค่า default ของ Linux จะรีเซ็ตความเร็วของ TCP เองทุกครั้งที่ connection
    ว่างไปสักพัก ขัดกับความพยายามระดับแอปโดยตรง
  - `net.ipv4.tcp_congestion_control = bbr` (ต้องตั้ง `net.core.default_qdisc = fq` คู่กัน)
  - `net.ipv4.tcp_fastopen = 3`
- **เส้นทางเครือข่ายสำรองคู่ขนาน (multi-homing)** ถ้าความนิ่งสำคัญกว่าค่าเช่า

---

## 7. Deploy ให้ปลอดภัย (ไม่เกี่ยวความเร็วโดยตรง แต่ป้องกันดาวน์ไทม์)

- Build/แพ็กจาก commit ที่ระบุแน่นอน ไม่ใช่ working directory ที่แก้ค้างอยู่
- รัน test suite เต็มชุดบนเครื่องปลายทางก่อนสลับ symlink — ถ้า test พังห้ามสลับ ของเดิมยังทำงาน
  ต่อได้ปกติ
- สลับด้วย symlink (blue-green แบบง่าย) ไม่ใช่เขียนทับไฟล์เดิมตรงๆ — ย้อนกลับได้ทันทีถ้าพัง
- เช็ค health หลัง restart แล้ว **rollback อัตโนมัติ**ถ้า service ไม่ขึ้นภายในเวลาที่กำหนด
- บันทึก "จะ rollback ไปที่ไหน" ไว้เป็นอันดับแรกสุดของ pipeline ก่อนขั้นตอนอื่นที่อาจพังได้

---

## 8. Environment Variables — Template (ไม่ใช่ค่าจริง)

⚠️ **นี่คือชื่อตัวแปรกับความหมาย ไม่ใช่ไฟล์ .env จริงของระบบนี้** — ค่าจริงมีรหัสผ่านและ
token อยู่ ไม่ควร copy ไปโปรเจกต์อื่น ใส่ค่าของโปรเจกต์ใหม่เองตามช่องว่างนี้

```bash
# ===== Auth (ตั้งค่าเอง ห้ามใช้ค่าเดโม) =====
ADMIN_USERNAME=changeme
ADMIN_PASSWORD=changeme_use_a_real_random_password

# ===== แจ้งเตือนเหตุขัดข้องนอกช่องทางหลัก (ถ้ามี) =====
# สร้าง bot token ใหม่เองผ่าน Telegram BotFather ห้ามใช้ค่าจากระบบอื่น
ALERT_TELEGRAM_BOT_TOKEN=
ALERT_TELEGRAM_CHAT_ID=

# ===== พฤติกรรมยืนยันว่าข้อความที่ส่งไปแสดงผลจริง (ดู 4.4) =====
SQUARE_VERIFY_SENDS=1
# ระยะเวลาหลังส่งที่ยัง "ตรวจสอบซ้ำ" ว่าข้อความยังโชว์อยู่ (ms)
SQUARE_VERIFY_DELAY_MS=2000
# resend อัตโนมัติถ้าข้อความหายไปจากห้องหลังส่งสำเร็จ (เปิดหลังพิสูจน์ read-back แล้ว)
SQUARE_RESEND_WHEN_INVISIBLE=0
REPLY_DEFENSE_MAX_RESENDS=2
# suffix มองไม่เห็นต่างกันทุกครั้งที่ตอบข้อความซ้ำในห้องเดิม / กัน echo คำตอบตัวเอง
REPLY_UNIQUIFY=0
AUTOMATIC_REPLY_ECHO_TTL_MS=30000

# ===== จำกัดอัตราการส่งของบัญชีเอง กันโดนแพลตฟอร์มมองว่าผิดปกติ =====
# ผูกกับบัญชีที่ "ส่งจริง" ไม่ใช่บัญชีที่ตรวจจับ (ดู 3.4)
SEND_MAX_PER_WINDOW=20
SEND_WINDOW_MS=60000

# ===== เพดานทรัพยากรระบบ (ดู system-load monitor) =====
SYSTEM_CPU_LIMIT_PERCENT=80
SYSTEM_MEMORY_LIMIT_PERCENT=80
SYSTEM_EVENT_LOOP_LIMIT_MS=100
SYSTEM_MONITOR_INTERVAL_MS=5000
SYSTEM_ALERT_SUSTAINED_SAMPLES=3
SYSTEM_ALERT_RECOVERY_SAMPLES=3

# ===== เซิร์ฟเวอร์ =====
PORT=8790
DB_PATH=data/app.db

# ===== Fast-poll (ดู 1.2) =====
SQUARE_FAST_POLL=1
# floor = 100ms ในโค้ด; 50 หรือ 0 ต้องเปิด SQUARE_FAST_POLL_ALLOW_50MS / ALLOW_ZERO_MS ควบคู่
SQUARE_FAST_POLL_INTERVAL_MS=100
# hard-cap = 1 ในโค้ด และ "จำนวนห้องที่เปิดได้ต่อบอท" ผูกจากค่านี้ (ดู 4.3)
SQUARE_FAST_POLL_MAX_ROOMS=1
# บังคับเป็น 1 ในโค้ด: หนึ่ง cursor ต่อ bot + ห้อง (ห้ามเพิ่ม worker ซ้อน)
SQUARE_FAST_POLL_WORKERS=1
# จำนวนรอบสูงสุดที่ยอมให้ "อ่านประวัติเก่าทิ้ง" ก่อนเริ่มส่งจริง ตอนเพิ่งเปิดช่องทางใหม่
SQUARE_FAST_POLL_INITIAL_DRAIN_LIMIT=10
# timeout ต่อการ fetch หนึ่งครั้ง กัน connection ค้างตลอดกาล (ดู 1.5)
SQUARE_FAST_POLL_FETCH_TIMEOUT_MS=15000
# หน้าต่างเวลาย้อนหลังที่ใช้นับว่าช่องทางไหน "คึกคัก" (ms)
SQUARE_FAST_POLL_ACTIVITY_WINDOW_MS=900000
# ห่างกันอย่างน้อยเท่าไหร่ถึงจะประเมินเลือกช่องทางใหม่ (ms)
SQUARE_FAST_POLL_RESELECT_INTERVAL_MS=60000

# ===== Push/long-poll watchdog (ดู 4.1, 4.2) =====
SQUARE_IDLE_DELAY_MS=20
SQUARE_STALL_MS=8000
SQUARE_STALL_RECOVERY_COOLDOWN_MS=15000
SQUARE_STALL_ESCALATE_AFTER=3

# ===== HTTP/2 connection pool (ดู 3.1, 3.2) =====
LINE_H2_LANES=6
# 0 = ปิด ไม่กันเลนไว้ ต้องวัดผลจริงก่อนเปิด (ดู 3.2)
LINE_H2_SEND_RESERVED_LANES=0
LINE_H2_LANE_MAX_AGE_MS=900000
LINE_H2_LANE_RECYCLE_GAP_MS=60000

# ===== SEND lane policy (ดู 3.6) — ทุกค่ามาจาก incident จริง, A/B test ทีละตัว =====
LINE_H2_SEND_SLOW_FLOOR_MS=23            # ประวัติ 28→23→20→23; ห้ามลดจาก sample ช่วงสั้น
LINE_H2_SEND_SLOW_RATIO=1.5
LINE_H2_SEND_SLOW_COOLDOWN_MS=15000
LINE_H2_SEND_PIN_ENTER_MS=21
LINE_H2_SEND_PIN_EXIT_MS=23
LINE_H2_SEND_SAMPLE_WINDOW=7
LINE_H2_SEND_JITTER_WEIGHT=0.35

# ===== แบ่งบอทหลาย process (ถ้าใช้ — ดู 3.x ของ ARCHITECTURE.md/PROJECT-BLUEPRINT.md) =====
# ปล่อยว่าง = process เดียวดูแลทุก owner (พฤติกรรมเดิม)
WORKER_ASSIGNMENT_MODE=                  # "balanced-sticky" = แบ่ง owner ใหม่ไปฝั่งที่บอทน้อยกว่า
WORKER_ASSIGNMENT_WORKERS=               # primary,shard-b (ตัวแรก = primary; ต้องมี ≥2)
WORKER_TOPOLOGY_FILE=                    # หรือคุมทั้ง topology จากไฟล์ JSON เดียว
CONTROL_PLANE_TOKEN=                     # ≥32 ตัวอักษร เหมือนกันทุก process ถ้าแยก

# ===== ความช้าที่ยอมรับได้ก่อนแจ้งเตือน (ดู 5.1) =====
INBOUND_SLOW_MS=400

# ===== กันตอบซ้ำ (ดู 1.4) =====
REPLY_CLAIM_TTL_MS=600000
REPLY_CLAIM_MAX=50000

# ===== ป้องกัน regex อันตราย/ข้อความยาวเกิน =====
RULE_REGEX_MAX_PATTERN=512
RULE_MATCH_MAX_TEXT=4096
RULE_REPLY_MAX_TEXT=4096

# ===== secret ที่ใช้เชื่อม process ภายในกันเอง (ถ้าแยก process ส่ง/รับ) =====
# สร้างค่าสุ่มใหม่เอง ห้าม copy จากระบบอื่น
DISPATCH_TOKEN=
```

---

## 9. บทเรียน/กับดักที่เจอมาจริง (สรุปสั้น กันพลาดซ้ำ)

1. **"ยิ่งขนานเยอะยิ่งเร็ว" ไม่จริงเสมอไป** เมื่อทรัพยากรที่ใช้ร่วมกันมีจำกัด — วัดก่อนเชื่อ
2. **จัดลำดับความสำคัญด้วยประวัติที่ผ่านมา มีจุดบอด** กับสิ่งที่ปกติเงียบแต่บางครั้งสำคัญมาก
   (แก้ด้วยการจำกัดจำนวนแทนการจัดอันดับ — ดู 1.2)
3. **Connection ที่ค้างแบบไม่ error คือของจริง ไม่ใช่ทฤษฎี** ต้องมี timeout ทุกจุดที่รอผลจาก
   ภายนอก
4. **ตัวเลขที่ดูดีจากตัวอย่างน้อยๆ ช่วงเดียว หลอกได้ง่ายมาก** — ทีมนี้เคยพลาดมาแล้ว 2 ครั้ง
   จากการเชื่อผลช่วงเดียว
5. **ยิ่งจำนวนบัญชี/instance ที่ยิงพร้อมกันเยอะ ยิ่งต้องระวังแพลตฟอร์มปลายทางมองว่าผิดปกติ**
   — ไม่มีสูตรตายตัวว่าแค่ไหนปลอดภัย ต้องดูสัญญาณเตือนของแพลตฟอร์มนั้นๆ เอง
6. **ค่าคงที่สองตัวที่ต้องเท่ากันเพื่อให้ระบบถูกต้อง ต้องผูกกันในโค้ดจริง** ไม่ใช่แค่คอมเมนต์
   เตือนกัน — บั๊กที่หายไปเงียบๆ ตอนมีคนแก้ทีหลังคือของจริง
