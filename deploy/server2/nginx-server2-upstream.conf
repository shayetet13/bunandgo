# Standby only. Defining this upstream does not send traffic to Server 2;
# cutover happens only when the /api and /ws proxy_pass directives explicitly
# select this name.
upstream linebot_backend_server2 {
    server 10.77.0.2:8791 max_fails=2 fail_timeout=5s;
    keepalive 32;
}
